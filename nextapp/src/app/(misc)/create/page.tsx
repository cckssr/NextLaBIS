import { Title } from "@mantine/core";

/**
 * Page for creating new entities at various levels.
 * To be implemented.
 *
 * The page will provide a user interface for creating new spaces, projects,
 * collections, or objects. The base insertion point can be selected.
 * Main idea is to have a form, where users can input necessary details and
 * create new objects in a streamlined manner. Maybe even with sequences or
 * other template functions.
 */
export default function CreationPage() {
  return <Title order={1}>Creation Page - To be implemented</Title>;
}
