import { Title } from "@mantine/core";

/**
 * Page for user-related functionalities
 * To be implemented.
 *
 * The page will provide a user interface for viewing and managing user
 * information, preferences, and possibly roles or permissions.
 * The user can update their name, email, password, zenodo API key, and
 * maybe other custom settings (stored in special object or cookie).
 */
export default function UserPage() {
  return <Title order={1}>User Page - To be implemented</Title>;
}
