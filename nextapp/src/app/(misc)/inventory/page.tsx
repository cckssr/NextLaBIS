import { Title } from "@mantine/core";

/**
 * Page for overview over all Inventory entities
 * To be implemented.
 *
 * The page will provide a user interface for viewing and managing all inventory
 * entities such as spaces, projects, collections, and objects. The main idea is
 * to have a dashboard-like view where users can see summaries, statistics, and
 * maybe perform bulk actions on inventory items.
 */
export default function InventoryOverviewPage() {
  return <Title order={1}>Inventory Overview Page - To be implemented</Title>;
}
