import { Title } from "@mantine/core";

/**
 * Page for application settings
 * To be implemented.
 *
 * The page will provide a user interface for viewing and modifying the general
 * openbis settings. All settings from the current UI should be available here.
 * Also, the type creation and management can be integrated here.
 */
export default function SettingsPage() {
  return <Title order={1}>Settings Page - To be implemented</Title>;
}
